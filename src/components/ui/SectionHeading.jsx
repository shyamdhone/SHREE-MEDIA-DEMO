export function SectionHeading({ eyebrow, title, description, align = 'left' }) { return <div className={`max-w-3xl ${align === 'center' ? 'mx-auto text-center' : ''}`}>{eyebrow && <p className="mb-4 text-xs font-semibold uppercase tracking-[0.18em] text-muted">{eyebrow}</p>}<h2 className="font-display text-4xl font-semibold leading-[0.98] tracking-[-0.06em] text-ink sm:text-5xl">{title}</h2>{description && <p className="mt-5 text-base leading-relaxed text-muted">{description}</p>}</div> }
